import React from 'react';
import { BoltIcon } from '@heroicons/react/24/outline';

/**
 * Simple brand logo icon used on authentication forms.
 */
export default function LogoIcon({ className = '' }) {
  return <BoltIcon className={className} />;
}

